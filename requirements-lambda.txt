# Runtime-only dependencies — what the Lambda container installs.
#
# WHY A SECOND FILE: requirements.txt also carries the corpus-collection
# tools (playwright, pdfplumber, beautifulsoup4). Those run once, offline, on
# a developer machine to rebuild the corpus; the container never calls them.
# Installing them anyway added roughly 150MB to the image and to every push.
#
# Keep this list in sync with what the runtime path actually imports:
#   pipeline/*, gate/{gate,topics,drafting}.py, opensearch/{client,config,
#   search,local_search,index_mapping}.py, handler.py

# Embeddings. bge-small at 384 dims; MIN_SIM = 0.72 is calibrated against
# this model, so changing it invalidates the threshold.
fastembed==0.8.0
onnxruntime==1.29.0
numpy==2.5.1

# The Cedar policy engine.
cedarpy==4.8.7

# AWS: SigV4 signing for bedrock-mantle, and the DynamoDB trail.
boto3==1.43.90

# OpenSearch retrieval. requests-aws4auth signs those requests — client.py
# imports it directly, so it is runtime, not optional.
opensearch-py==3.2.0
requests==2.32.5
requests-aws4auth==1.3.2
